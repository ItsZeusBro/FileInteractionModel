export class Clusive{
    constructor(orientation, n, m){
        this.clusivity;
        if (clusive=="in"){
            this.clusivity = clusivity;
        }else if (clusive=="ex"){
            this.clusivity = clusivity;
        }else{
            throw Error("Clusivity requires a prefix");
        }
        
    }
    clusivity(){
        return this.clusive;
    }
    
    exclude(path){
        //truncate file contents to
    }
}